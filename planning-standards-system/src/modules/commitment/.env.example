PORT=3002
DB_HOST=localhost
DB_PORT=5444
DB_USERNAME=postgres
DB_PASSWORD=steph123gd
DB_NAME=commitment-db

# Cross-service URLs for downstream API validation
SERVICE_CATALOGUE_URL=http://localhost:3000
KPI_SLA_URL=http://localhost:3001

# Kafka broker connection for TT5 Transactional Outbox (AuditDispatcherService)
# Controls where the Kafka Publisher Worker delivers pending audit events to ARMS
KAFKA_BROKER=localhost:9092
KAFKA_CLIENT_ID=pss-commitment
