
export enum CommitmentStatus {
  DRAFT = 'Draft',
  LOCKED = 'Locked',
  REVISION_REQUESTED = 'Revision Requested',
}

export enum CommitmentItemUnit {
  DAYS = 'DAYS',
  PERCENT = 'PERCENT',
  COUNT = 'COUNT',
}
